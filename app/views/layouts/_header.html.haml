%nav.navbar.navbar-main
  .container-fluid
    .navbar-inner
      .navbar-brand.logo
        = link_to "Gallery" , root_path

      .navbar-brand.pull-right
        -if !current_user.nil?
          .avatar
            = avatar_small_thumb(current_user)
      .navbar-brand.hello.pull-right
        = hello
%nav.navbar.navbar-menu
  .container-fluid
    .navbar-inner

      %ul.nav.nav-tabs
        %li
          - if  user_signed_in?
            = link_to "#{t 'header.create_post'}", new_post_path
        %li
          = link_to "#{t 'header.event_list'}", events_path

        %ul.nav.nav-tabs
          %li.dropdown
            %li.dropdown
              %a.dropdown-toggle{"data-toggle" => "dropdown", :href => "#"}
                #{t 'content.leng'}
                %span.caret
              %ul.dropdown-menu
                %li
                  = link_to "English", locale: 'en'
                %li
                  = link_to "Русский", locale: 'ru'

          -if user_signed_in?
            %li
              = link_to "#{t 'header.edit_profie'}", edit_user_registration_path
            %li
              = link_to("#{t 'header.log_out'}", destroy_user_session_path, :method => :delete)
          -else
            %li
              = link_to "#{t 'header.log_in'}", new_session_path(:user)
            %li
              = # link_to "Sign up", new_registration_path(:user)